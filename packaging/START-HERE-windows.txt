CLOUD STORAGE SERVER 0.9.12 — CONNECTION, DISKS AND SECURITY
================================

Обычный запуск
--------------
1. Запустите CloudStorage-Server-Setup-0.9.12-windows-x64.exe от администратора.
2. Установщик создаст и автоматически запустит службу CloudStorageServerCore.
3. Откройте Cloud Storage Server Manager из меню «Пуск».
4. В разделе «Пользователи» создайте человеку логин с временным паролем.
5. Если файлы нужно сначала принимать на SSD, назначьте этому диску роль «Кэш».
6. Docker и SSH настраиваются в «Система» → «Docker и SSH»; удалённый вход по паролю
   по умолчанию не включается.

Вкладка «Приём» покажет сетевую загрузку на SSD и последующий проверяемый перенос на HDD.
Вкладка «Отправка» показывает текущие и завершённые скачивания клиентов.

Нативное Go-ядро работает отдельной Windows-службой, следит за внутренним API и не
останавливается при закрытии Manager.

Обновление запускается во вкладке «Обновления». База, пользователи, настройки и файлы
сохраняются. Деинсталлятор также сохраняет их, пока пользователь отдельно и дважды не
подтвердит полное удаление хранилища.

Важно
-----
Desktop Client, защищённый LAN и отдельный удалённый HTTPS-вход входят в комплект. Удалённый
доступ по умолчанию выключен и открывает только клиентский API; админка остаётся на 127.0.0.1.
Программа не использует UPnP: примените VPN или вручную направьте один TCP-порт на сервер.

Android/iPhone-клиент использует тот же логин. Каждый новый телефон или компьютер сначала
появляется в Manager со статусом «Ожидает» и требует отдельного подтверждения администратора.

Программа не форматирует диски. Управляемые данные она помещает только в собственный каталог
CloudStorageData. В «Настройки» → «Диагностика» доступны автоматический мониторинг и полная
проверка объектов по SHA-256 без скрытого удаления или исправления файлов.

При инциденте в «Настройки» → «Сервер» можно включить глобальный аварийный режим только
чтения. В «Резервные копии» готовый снимок умеет проверяемо восстановить отсутствующие или
повреждённые объекты, не перезаписывая исправные и более новые файлы.

Полный этап хранения и автоматизации завершён: у резервного диска можно включить расписание,
задать число сохраняемых снимков и выбрать основной диск для пробного восстановления. Старые
копии удаляются только после успешной проверки базы, размера и SHA-256 всех объектов.
