# План разработки

## Выполнено

### Этап 1 — Server Manager Beta 0.1

- интерфейс, реальные диски и сохранение политик;
- необязательная первоначальная настройка;
- состояния паузы, обслуживания и отключения;
- отсутствие скрытых изменений данных.

### Этап 2 — Server Core Beta 0.2

- независимый фоновый процесс, Windows Service Host и systemd unit;
- локальный API v1, SQLite, пользователи и личные пространства;
- одноразовые приглашения, Argon2id и подтверждение устройств;
- атомарная загрузка, скачивание, версии и мягкое удаление;
- применение приоритета, заполнения и резерва подключённых дисков.

### Этап 3 — Desktop Client Alpha 0.3

Завершён настольный клиент:

- отдельный кроссплатформенный Client App;
- код подключения, пароль, pending-состояние и подтверждение;
- просмотр пространств и файлов;
- фоновая загрузка/скачивание и локальный офлайн-каталог;
- автоматическая проверка подключения;
- защищённое локальное хранение токена;
- вкладка вопросов и ответов в Server Manager и Desktop Client;
- локальный помощник с общей SQLite-базой знаний и учётом вопросов без ответа;
- отдельный HTTPS-вход для LAN, постоянный сертификат и закрепление отпечатка;
- обнаружение сервера в локальной сети и полное приглашение для клиента;
- устойчивые передачи с паузой, повтором после обрыва и продолжением после перезапуска;
- индекс офлайн-кэша с SHA-256 и безопасным разрешением конфликтов;
- подключение каталога к Проводнику Windows и файловому менеджеру Linux;
- автозапуск, системный трей и Client Installer.

### Этап 4 — хранение и автоматизация

- дополнительные диски, правила заполнения и режимы обслуживания;
- проверяемый перенос данных с сохранением исходной страховочной копии;
- ручные и автоматические резервные снимки;
- зеркальные реплики, автоматическое восстановление и чтение при отказе основного объекта;
- постоянное расписание снимков и политика хранения — готовы в Alpha 0.5.0a3;
- пробное восстановление базы и каждого объекта на основной диск — готово в Alpha 0.5.0a3;
- безопасная ротация только успешно проверенных снимков — готова в Alpha 0.5.0a3.

### Этап 5 — надёжность сервера и удалённый доступ

- постоянная быстрая диагностика, история инцидентов и автоматическое закрытие устранённых проблем — готовы в Alpha 0.5.0a1;
- полная проверка текущих объектов, версий и зеркал по SHA-256 — готова в Alpha 0.5.0a1;
- восстановление прерванных диагностических заданий — готово в Alpha 0.5.0a1;
- проверяемое восстановление отсутствующих/повреждённых объектов из снимка — готово в Alpha 0.5.0a2;
- управляемый глобальный аварийный режим только чтения — готов в Alpha 0.5.0a2;
- отдельный удалённый HTTPS-вход с белым списком клиентского API и полным запретом административных маршрутов;
- явное управление внешним доступом и временным удалённым подключением по коду в Server Manager;
- ограничение частоты запросов и журнал внешних обращений с адресом, маршрутом и результатом;
- ручная модель настройки VPN/роутера без скрытого UPnP или изменения firewall;
- несколько независимых серверных профилей и раздельные защищённые токены в Desktop Client;
- автоматическая миграция старого одиночного профиля и токена — готовы в версии 0.5.0.

## В работе

Этап 7 завершён в версии 0.7.0. Этап 8 начат в версии 0.8.0: общий Android/iPhone-клиент и многокомпьютерный вход уже подключены к Core API.

## Следующие этапы по Idea #003

### Этап 6 — развитие настольной экосистемы

- базовый интерфейс tunnel-provider и интеграция zrok без открытия портов роутера — готовы в Alpha 0.6.0a1;
- отдельный loopback-шлюз с default deny, автоматическим перезапуском и диагностикой процесса — готов в Alpha 0.6.0a1;
- интернет-сессии по логину/Argon2id-паролю и токену подтверждённого устройства — готовы в Alpha 0.6.0a1;
- безопасный реестр встроенных провайдеров с декларативными манифестами и живым перезапуском без остановки Core — готов в Alpha 0.6.0a2;
- обезличенный диагностический ZIP с системными счётчиками и SHA-256, но без базы, логов, файлов, идентификаторов и секретов — готов в Alpha 0.6.0a2;
- постоянный движок правил с cooldown, системными шаблонами, историей запусков и действиями notify/quick scan/read-only — готов в 0.6.0;
- расширенный движок с расписанием, событиями restore/mirror/maintenance/device, полной диагностикой, backup, восстановлением зеркала, перезапуском tunnel-provider, preview и управлением планировщиком — готов в 0.7.2;
- серверный SSD staging, постоянные очереди «Приём»/«Отправка», проверяемый перенос на HDD и повтор после ошибки — готов в 0.7.3;
- центр уведомлений и встроенные провайдеры Manager Inbox/System Log без внешней сети и стороннего кода — готовы в 0.6.0;
- расширенные проверки безопасности, TLS, pairing, просроченных передач и backup-политик с управляемой remediation — готовы в 0.6.0;
- **этап 6 закрыт полностью в версии 0.6.0.**

### Этап 7 — Windows-установщики, диски и обновления

- полный Server Setup: Manager, Core, автоматическая Windows-служба, инициализация и частные firewall-правила — готов в 0.7.0;
- полный Client Setup со встроенным WinFsp и компонентом настоящего сетевого диска — готов в 0.7.0;
- отдельная буква диска для каждого серверного профиля, ленивое скачивание и запись на сервер — готовы в 0.7.0;
- подписанные Ed25519-манифесты, HTTPS, проверка размера/SHA-256 и установка из Manager/Client — готовы в 0.7.0;
- сохранение серверных и клиентских данных при обновлении/обычном удалении — готово в 0.7.0;
- **этап 7 закрыт полностью в версии 0.7.0.**

### Этап 8 — мобильные приложения, финальный этап

- единый аккаунт с Argon2id-паролем для Desktop/Android/iPhone и обязательным подтверждением каждого нового устройства — готов в 0.8.0;
- Mobile Client: пространства, папки, документы, фотографии, удаление и скачивание — готов в 0.8.0;
- постоянная возобновляемая очередь, ограниченная фоновая обработка ОС и офлайн-копии — готовы в 0.8.0;
- Mobile Server Control: безопасная сводка, подтверждение/отзыв устройств и read-only только для роли `admin` — готов в 0.8.0;
- публичная Android-сборка, магазинная подпись и iOS TestFlight/App Store — следующие задачи этапа 8;
- автоматическая фотосинхронизация всей медиатеки и дополнительные опасные административные операции с повторным подтверждением — следующие задачи этапа 8.
